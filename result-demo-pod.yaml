apiVersion: v1
kind: Pod
metadata:
  name: result-app-demo
  labels:
    name: result-app-demo
    app: demo-app
spec:
  containers:
    - name: result-app
      image: kodekloud/examplevotingapp_result:v1
      ports:
        - containerPort: 80
